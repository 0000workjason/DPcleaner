# ── Python（backend）──────────────────────────
.venv/
.venv-cpu/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# PyInstaller build output for the packaged backend sidecar
backend/build/
backend/dist_cpu/
*.spec

# ── Tauri sidecar binary（用 PyInstaller 產生，不進 git，太大）───
src-tauri/binaries/

# 產生物：合成測試圖（backend/tests/synthetic.py 可重建）
**/tests/_selftest/

# Embedding 快取在 ~/.dpcleaner_cache.sqlite（repo 外）

# ── 前端（desktop）────────────────────────────
node_modules/
dist/
dist-ssr/
*.local

# ── Logs ─────────────────────────────────────
*.log
logs/

# ── 編輯器 / OS ───────────────────────────────
**/.vscode/*
!**/.vscode/extensions.json
.idea/
.DS_Store

# ── Claude Code 執行期暫存 ─────────────────────
.claude/
