# ── Python (backend) ─────────────────────────
.venv/
.venv-cpu/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# PyInstaller build output for the packaged backend sidecar
backend/build/
backend/dist_cpu/
*.spec

# ── Tauri sidecar binary (PyInstaller output; too large for git) ──
src-tauri/binaries/

# Generated: synthetic test images (rebuild via backend/tests/synthetic.py)
**/tests/_selftest/

# Embedding cache lives at ~/.dpcleaner_cache.sqlite (outside the repo)

# ── Frontend (desktop) ───────────────────────
node_modules/
dist/
dist-ssr/
*.local

# ── Logs ─────────────────────────────────────
*.log
logs/

# ── Editors / OS ─────────────────────────────
**/.vscode/*
!**/.vscode/extensions.json
.idea/
.DS_Store

# ── Claude Code scratch ──────────────────────
.claude/
dist-portable/
